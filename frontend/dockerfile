# Use an appropriate base image for your frontend, such as node.js
FROM node:16

# Set the working directory inside the container
WORKDIR /app

# Copy the frontend files to the container
COPY package*.json ./

# Install dependencies
RUN npm install

COPY . .

ENV PORT=3000

# Expose the port on which your frontend server will run (if applicable)
EXPOSE 3000

# Define the command to run your frontend application
CMD ["npm", "start"]
